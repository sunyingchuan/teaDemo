<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 
    <typeAliases>
       <typeAlias type="com.ucar.team.seven.tea.system.entity.SysUser" alias="sysUser"/>
    </typeAliases>
    -->
    <!-- 
         为mapper文件中的参数类型,
         返回值类型定义别名
         假如是采用package元素定义,
         那默认类型的别名为类名并且
         第一个单词的首字母小写.
     -->
    <typeAliases>
        <package name="com.ucar.team.seven.tea.system.entity"/>
    </typeAliases>
</configuration>
